class Tenant::Catalogue::FavouritesController < Tenant::CatalogueController
  def index
    # if params[:path].present?
    #   navigate_path
    # else
    #   @segments = []
    # end

    # @media_item = @current_folder.present? ? Tenant::MediaItem.new(folder: @current_folder) : Tenant::MediaItem.new
  end


end
